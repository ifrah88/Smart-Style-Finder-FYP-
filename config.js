// Backend URL — ngrok tunnel to the Flask server.
// Update this to your current ngrok URL, then rebuild the APK.
// Or change it without rebuilding via Profile → Server Settings in the app.
export const API_BASE_URL = "https://kitty-folk-elope.ngrok-free.dev";
